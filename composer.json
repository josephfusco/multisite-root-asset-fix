{
    "name": "josephfusco/multisite-root-asset-fix",
    "description": "WordPress plugin to fix asset URLs in multisite subdirectory installations",
    "type": "wordpress-plugin",
    "license": "GPL-2.0-or-later",
    "require": {
        "php": ">=7.2",
        "composer/installers": "^2.0"
    },
    "keywords": [
        "wordpress",
        "wordpress-plugin",
        "multisite",
        "assets",
        "url-fix"
    ],
    "config": {
        "allow-plugins": {
            "composer/installers": true
        }
    }
}
